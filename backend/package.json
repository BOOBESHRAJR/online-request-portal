{
  "name": "backend",
  "version": "1.0.0",
  "description": "Online Information Request Portal Backend",
  "main": "server.js",
  "scripts": {
    "start": "cross-env NODE_OPTIONS=--openssl-legacy-provider node server.js",
    "dev": "cross-env NODE_OPTIONS=--openssl-legacy-provider nodemon server.js",
    "build": "npm install --prefix ../frontend && npm run build --prefix ../frontend"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "express-mongo-sanitize": "^2.2.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.2.1",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
